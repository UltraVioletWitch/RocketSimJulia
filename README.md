Simulation of L1 rocket written in Julia for improved speed over MATLAB. 
Also implements zero-mean gaussian noise to the data to simulate sensor readings, then uses Kalman filter to calculate the true value from the sensor readings.
